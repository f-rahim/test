import React from "react";

function State_test(props){

    return(
        <div className="state">
            <h2>My name is: {props.name} and my roll is: {props.roll}</h2>
        </div>
    );
}

export default State_test;